import { createTheme } from "@material-ui/core/styles"

const theme = createTheme( {
   palette: {
      type: "dark",
      primary: {
         main: "#1976d2"
      },
      secondary: {
         main: "#64dd17"
      }
   }
} )

export default theme